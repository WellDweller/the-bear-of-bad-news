using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestMinigame : Minigame
{
    // Start is called before the first frame update
    void Start()
    {
        result.text = "it is un-bear-able";
        result.score = 1;    
    }
}
